import styled from 'styled-components/native';
import { Typography } from '../typography';

export const ConnectedContainer = styled.View`
    align-items: center;
    padding: 10px 0;
    background-color: #a00;
`;
export const ConnectedText = styled(Typography)`
    color: white;
    font-size: 13px;
`;
